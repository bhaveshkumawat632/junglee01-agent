<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Autonomous AI Agent Platform</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { background: #0a0a0f; color: #ffffff; font-family: 'Courier New', monospace; overflow-x: hidden; }
.header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 20px; text-align: center; border-bottom: 3px solid #00ff88; }
.header h1 { font-size: 2em; text-shadow: 0 0 10px #00ff88; }
.header p { color: #00ff88; margin-top: 5px; }
.container { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; padding: 20px; max-width: 1600px; margin: 0 auto; }
.panel { background: #1a1a2e; border: 1px solid #00ff88; border-radius: 5px; padding: 15px; box-shadow: 0 0 15px rgba(0, 255, 136, 0.2); }
.panel h2 { color: #00ff88; margin-bottom: 10px; font-size: 1.2em; border-bottom: 1px solid #00ff88; padding-bottom: 5px; }
.terminal { background: #000000; border: 1px solid #00ff88; border-radius: 3px; padding: 10px; height: 300px; overflow-y: auto; font-size: 12px; }
.terminal-line { margin: 2px 0; white-space: pre-wrap; word-break: break-all; }
.terminal-line.info { color: #00ff88; }
.terminal-line.error { color: #ff4444; }
.terminal-line.warning { color: #ffaa00; }
.terminal-line.system { color: #00aaff; }
.terminal-line.output { color: #ffffff; }
.input-group { display: flex; gap: 10px; margin-top: 10px; }
.input-group input { flex: 1; background: #000000; border: 1px solid #00ff88; color: #00ff88; padding: 8px; font-family: 'Courier New', monospace; font-size: 14px; }
.input-group button { background: #00ff88; color: #000000; border: none; padding: 8px 20px; cursor: pointer; font-weight: bold; font-family: 'Courier New', monospace; }
.input-group button:hover { background: #00cc66; }
.model-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 10px; margin-top: 10px; }
.model-card { background: #16213e; border: 1px solid #00ff88; border-radius: 3px; padding: 10px; text-align: center; }
.model-card .name { color: #00ff88; font-weight: bold; margin-bottom: 5px; }
.model-card .status { font-size: 11px; color: #ffaa00; }
.model-card.active { border-color: #00ff88; box-shadow: 0 0 10px rgba(0, 255, 136, 0.5); }
.model-card.active .status { color: #00ff88; }
.stats { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; margin-top: 10px; }
.stat { background: #16213e; border-left: 3px solid #00ff88; padding: 8px; }
.stat-label { font-size: 11px; color: #888; }
.stat-value { font-size: 18px; color: #00ff88; font-weight: bold; }
.tasks-list { max-height: 200px; overflow-y: auto; margin-top: 10px; }
.task-item { background: #16213e; border-left: 3px solid #00ff88; padding: 8px; margin: 5px 0; font-size: 12px; }
.task-item.completed { border-left-color: #00ff88; }
.task-item.failed { border-left-color: #ff4444; }
.task-item.running { border-left-color: #ffaa00; }
@media (max-width: 768px) {
  .container { grid-template-columns: 1fr; }
}
</style>
</head>
<body>
<div class="header">
  <h1>🤖 AUTONOMOUS AI AGENT PLATFORM</h1>
  <p>Real-time Execution • Multi-Model Orchestration • Live Terminal Streaming</p>
</div>
<div class="container">
  <div class="panel">
    <h2>🎯 Task Input</h2>
    <div class="input-group">
      <input type="text" id="taskInput" placeholder="Enter task for autonomous execution..." />
      <button onclick="submitTask()">EXECUTE</button>
    </div>
    <div class="model-grid">
      <div class="model-card" id="model-hermes">
        <div class="name">Hermes</div>
        <div class="status">● Standby</div>
      </div>
      <div class="model-card" id="model-gemini">
        <div class="name">Gemini CLI</div>
        <div class="status">● Standby</div>
      </div>
      <div class="model-card" id="model-kiro">
        <div class="name">Kiro CLI</div>
        <div class="status">● Standby</div>
      </div>
      <div class="model-card" id="model-aider">
        <div class="name">Aider</div>
        <div class="status">● Standby</div>
      </div>
      <div class="model-card" id="model-ollama">
        <div class="name">Ollama</div>
        <div class="status">● Standby</div>
      </div>
    </div>
    <div class="stats">
      <div class="stat">
        <div class="stat-label">Tasks Completed</div>
        <div class="stat-value" id="tasksCompleted">0</div>
      </div>
      <div class="stat">
        <div class="stat-label">Active Models</div>
        <div class="stat-value" id="activeModels">0</div>
      </div>
      <div class="stat">
        <div class="stat-label">Execution Time</div>
        <div class="stat-value" id="execTime">0s</div>
      </div>
      <div class="stat">
        <div class="stat-label">Success Rate</div>
        <div class="stat-value" id="successRate">100%</div>
      </div>
    </div>
  </div>
  <div class="panel">
    <h2>💻 Live Terminal Output</h2>
    <div class="terminal" id="terminal">
      <div class="terminal-line system">[SYSTEM] Autonomous AI Agent Platform v4 initialized</div>
      <div class="terminal-line system">[SYSTEM] Patterns: ReAct, StateGraph, AgentTool, Self-Eval, Retry, PersistentQueue, SkillLoader, HookDispatch</div>
      <div class="terminal-line info">[READY] Waiting for task input...</div>
    </div>
  </div>
</div>
<div class="container" style="margin-top: 20px;">
  <div class="panel">
    <h2>📋 Task Queue</h2>
    <div class="tasks-list" id="tasksList">
      <div class="task-item">No tasks yet</div>
    </div>
  </div>
  <div class="panel">
    <h2>🔗 Hook Status</h2>
    <div class="terminal" id="hookTerminal">
      <div class="terminal-line system">[HOOK] POST /hooks with Bearer token</div>
      <div class="terminal-line system">[HOOK] Ready for webhook dispatch</div>
    </div>
  </div>
</div>
<script>
let taskCount = 0;
let startTime = null;
let ws = null;

function appendTerminal(message, type = 'output') {
  const terminal = document.getElementById('terminal');
  const line = document.createElement('div');
  line.className = `terminal-line ${type}`;
  line.textContent = message;
  terminal.appendChild(line);
  terminal.scrollTop = terminal.scrollHeight;
}

function updateModelStatus(modelId, active) {
  const card = document.getElementById(`model-${modelId}`);
  const status = card.querySelector('.status');
  if (active) {
    card.classList.add('active');
    status.textContent = '● Running';
  } else {
    card.classList.remove('active');
    status.textContent = '● Standby';
  }
}

function updateTaskList(tasks) {
  const list = document.getElementById('tasksList');
  list.innerHTML = '';
  tasks.forEach(task => {
    const item = document.createElement('div');
    item.className = `task-item ${task.status}`;
    item.textContent = `[${task.task_id}] ${task.status} | score: ${task.score} | model: ${task.model} | steps: ${task.steps}`;
    list.appendChild(item);
  });
}

async function loadTasks() {
  try {
    const res = await fetch('/api/tasks');
    const data = await res.json();
    updateTaskList(data.tasks || []);
    taskCount = (data.tasks || []).length;
    document.getElementById('tasksCompleted').textContent = taskCount;
  } catch (e) {
    console.error('Failed to load tasks', e);
  }
}

async function connectWebSocket() {
  const wsUrl = (location.protocol === "https:" ? "wss://" : "ws://") + location.host + "/ws";
  try {
    ws = new WebSocket(wsUrl);
    ws.onopen = () => {
      appendTerminal('[SYSTEM] Connected to agent backend', 'system');
    };
    ws.onmessage = (event) => {
      const data = JSON.parse(event.data);
      switch(data.type) {
        case 'terminal_output':
          appendTerminal(data.output, data.level);
          break;
        case 'model_update':
          updateModelStatus(data.model, data.active);
          break;
        case 'task_update':
          if (data.status === 'completed' || data.status === 'failed') {
            loadTasks();
          }
          break;
      }
    };
    ws.onclose = () => {
      appendTerminal('[SYSTEM] WebSocket disconnected, reconnecting...', 'system');
      setTimeout(connectWebSocket, 3000);
    };
  } catch(e) {
    appendTerminal('[ERROR] WebSocket not supported', 'error');
  }
}

async function submitTask() {
  const input = document.getElementById('taskInput');
  const task = input.value.trim();
  if (!task) {
    appendTerminal('[ERROR] No task provided', 'error');
    return;
  }
  appendTerminal(`[TASK] ${task}`, 'system');
  appendTerminal('[EXEC] Starting autonomous execution...', 'info');
  try {
    const res = await fetch('/api/task', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({task, model: 'hermes'})
    });
    const data = await res.json();
    appendTerminal(`[TASK_ID] ${data.task_id}`, 'system');
    loadTasks();
  } catch (e) {
    appendTerminal('[ERROR] Failed to submit task', 'error');
  }
  input.value = '';
}

window.addEventListener('load', () => {
  connectWebSocket();
  loadTasks();
});
document.getElementById('taskInput').addEventListener('keypress', (e) => {
  if (e.key === 'Enter') submitTask();
});
setInterval(loadTasks, 5000);
</script>
</body>
</html>
